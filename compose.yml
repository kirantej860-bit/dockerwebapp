version: "3.9"

services:
  devopsdb:
    image: devopstej/automation:db
    ports:
      - "3306:3306"
    networks:
      - mynetwork

  application:
    image: devopstej/automation:app
    ports:
      - "1111:8080"
    deploy:
      replicas: 3
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge
